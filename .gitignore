# External SDK sources and internal-only notes — never versioned
references/

# Build artifacts
*.o
*.elf
/build/

# Local audio tooling binaries (FluidSynth reference synth, download zips) — not versioned
tools/bin/
__pycache__/
